# Copy to .env (gitignored) and fill in a devnet RPC URL.
# A Helius (or similar) devnet endpoint is strongly recommended — the public
# api.devnet.solana.com endpoint rate-limits getProgramAccounts heavily.
VITE_RPC_URL=https://api.devnet.solana.com
